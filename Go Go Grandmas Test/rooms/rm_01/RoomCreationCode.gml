globalvar enemyCount; // Creates enemyCount variable
enemyCount = 1; // Initial enemy count is how many enemies are on screen

globalvar canMoveOn; // Creates canMoveOn variable
canMoveOn = false; // Player cannot move on until all enemies are dead

globalvar totalEnemies; // Creates totalEnemies variable
totalEnemies = 5; // Total amount of enemies that will spawn on current level